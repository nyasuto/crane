# Crane Phase 3.6: Rocker-foot Kneed Walker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** McGeer 1990b の原機械（膝付き4相 hybrid + 円弧足の転がり）を derive レイヤーで導出し、リミットサイクルを発見・安定性を固有値で証明し、R→0 で Phase 3 検証済み点足 kneed に縮退することを示す。

**Architecture:** Phase 3 の `kneed.py`（4相機械・Hsu Chen 点質量）に「stance 足の転がり」だけを足した新モデル `models/rocker_kneed.py`。kneed.py の `_build()` を 5 箇所だけ変更（hip を円弧足上で転がらせ、衝突 pivot を点足→転がり接触点に）した自前 `_build()` を持つ。derive 層・kneed.py・rocker_compass.py は無改変。R→0 で点足 kneed に厳密縮退するのが最強の検証ゲート。

**Tech Stack:** Python 3.12 / uv / numpy / scipy / sympy / matplotlib / pytest / ruff

**Spec:** `docs/2026-06-16-rocker-foot-kneed-design.md`

---

## 物理モデル早見表（kneed.py との差分 = 5 箇所）

状態・相機械・質量モデル・knee-strike 定式化は Phase 3 kneed と同一。追加は「stance 足の転がり」のみ。

1. **hip を転がりに**: `C_st = [−R·θ_st, R]`、`hip = C_st − (L−R)·down(θ_st)`
   （kneed の `hip = [−L sinθ_st, L cosθ_st]` を置換。R→0 で一致）。
2. **stance 接触点**: `P_st = [−R·θ_st, 0]`（kneed の固定点 [0,0] を置換）。
3. **swing 円弧足の接触点**: `C_sw = knee_sw + (l_s−R)·down(θ_sh)`、`P_sw = [C_sw_x, 0]`
   （kneed の `foot_sw = knee_sw + l_s·down(θ_sh)` を heel-strike pivot として置換）。
4. **衝突 pivot の置換**: knee-strike の系全体 pivot `[0,0]→P_st`、heel-strike の系全体
   pre/post pivot `[0,0]→P_st`・`foot_sw→P_sw`。L_*_hip（hip 回り）は hip 記号がそのまま。
5. **パラメータに R 追加**。

stance 脚は両相とも単一剛体リンク長 L、転がりは hip 経由で全質点・両相（unlocked T3 / locked
T2=T3.subs(lock)）に自動継承。knee-strike は swing 脚内部（θ_sh−θ_th=0）で足形状と無関係。
両足同半径 R の幾何対称性から strike 面は `θ_st+θ_sw=0`（kneed/compass と同一、厳密）。
R→0 で Phase 3 点足 kneed (0.23859, −1.10959, −0.05715) に厳密縮退。

文献値（McGeer 1990b）は Task 1 で provenance 付き取得。**プランに数値は書かない**。

## File Structure

| パス | 責務 |
|---|---|
| `src/crane/references_mcgeer_knees.py` | McGeer 1990b 公表値（provenance、図読取ラベル、薄ければ None） |
| `src/crane/models/rocker_kneed.py` | rocker-foot kneed（kneed.py + 転がり、新規） |
| `src/crane/viz.py` | `rocker_kneed_joints` + `animate_rocker_kneed`（4セグメント + 円弧足） |
| `scripts/walk_rocker_kneed.py` | Phase 3.6 デモ |
| `tests/test_*` | 各 Task のゲート |

---

### Task 1: McGeer 1990b の文献調査（references_mcgeer_knees.py）

**Files:** Create `src/crane/references_mcgeer_knees.py`, `tests/test_references_mcgeer_knees.py`

**Web 調査タスク（Phase 3/3.5 Task 1 と同じ規律）。** 一次資料は McGeer (1990) "Passive Walking
with Knees", Proc. IEEE ICRA（Cornell/Ruina ラボ公開 PDF、6 ページ）。PDF は
`http://ruina.tam.cornell.edu/research/history/mcgeer_1990_passive_walking_knees.pdf`
（取得済み: `/tmp/crane_papers/McGeer90b.pdf`）。スキャンならページ画像化して読む
（`uv run --with pymupdf python -c "import fitz; d=fitz.open('/tmp/crane_papers/McGeer90b.pdf'); [d[i].get_pixmap(dpi=170).save(f'/tmp/m90b_{i+1}.png') for i in range(d.page_count)]"` → Read で読む）。

取得し、kneed の規約（m_h, m_t, m_s, l_t, l_s, b_t, b_s, R, gamma, g）へ変換（根拠を docstring）:
- rocker-foot kneed のパラメータ（足半径 R、各リンク長・質量分布、斜面 γ）
- 公表 gait 数値（不動点・step period・固有値）。**6 ページ会議論文なので薄い可能性大**。
  本文印刷値と図読取値を区別し、図のみは digitized ±X% ラベル。無い量は None + 理由。

既存 `src/crane/references_kneed.py`（Hsu Chen 点足 kneed）と `references_mcgeer.py`（McGeer 1990a）の
スタイルを踏襲。

- [ ] **Step 1: ゲートテストを書き FAIL 確認**

```python
# tests/test_references_mcgeer_knees.py
from crane import references_mcgeer_knees as ref


def test_mcgeer_knees_references_are_filled():
    assert "http" in ref.PROVENANCE
    for name in ["M_HIP", "M_THIGH", "M_SHANK", "L_THIGH", "L_SHANK", "B_THIGH", "B_SHANK", "R_FOOT"]:
        assert isinstance(getattr(ref, name), float), name
    assert isinstance(ref.GAMMA_GAIT, float)
    assert 0.0 < ref.GAMMA_GAIT < 0.3
    assert len(ref.SECTION_GUESS) == 3  # (θ_st, θ̇_st, θ̇_sw)
    assert isinstance(ref.PUBLISHED_STABLE, bool)
```

Run: `uv run pytest tests/test_references_mcgeer_knees.py -v` → FAIL（ModuleNotFoundError）

- [ ] **Step 2: Web/PDF 調査、references_mcgeer_knees.py 記入**

必須フィールド: `PROVENANCE`（URL + 取得日 2026-06-16）, `M_HIP`, `M_THIGH`, `M_SHANK`,
`L_THIGH`, `L_SHANK`, `B_THIGH`, `B_SHANK`, `R_FOOT`, `G`（float）, `GAMMA_GAIT`（float rad）,
`SECTION_GUESS`（3-tuple、変換根拠コメント。McGeer が不動点を印刷しなければ物理的に妥当な seed
= Phase 3 kneed 近傍を R 補正し「seed であり公表値でない」と明記）, `GAIT_TOLERANCE`（根拠コメント）,
`STEP_PERIOD: float | None`, `POINCARE_EIGENVALUES: tuple | None`, `PUBLISHED_STABLE: bool`。

- [ ] **Step 3: テスト pass + ruff(format & check) + Commit**

```bash
uv run pytest tests/test_references_mcgeer_knees.py -v && uv run ruff format . && uv run ruff check .
git add src/crane/references_mcgeer_knees.py tests/test_references_mcgeer_knees.py
git commit -m "feat: record McGeer 1990b kneed walker values with provenance"
```
（commit body にパラメータ変換と固有値の素性判断を記録。Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>）

---

### Task 2: rocker_kneed.py — 両相の転がり力学（TDD）

**Files:** Create `src/crane/models/rocker_kneed.py`, `tests/test_rocker_kneed_dynamics.py`

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_rocker_kneed_dynamics.py
import numpy as np
from scipy.integrate import solve_ivp

from crane import references_kneed as kref
from crane.models.rocker_kneed import RockerKneedParams, dynamics_unlocked, dynamics_locked, energy

P = RockerKneedParams(m_h=kref.M_HIP, m_t=kref.M_THIGH, m_s=kref.M_SHANK,
                      l_t=kref.L_THIGH, l_s=kref.L_SHANK, b_t=kref.B_THIGH, b_s=kref.B_SHANK,
                      R=0.2, gamma=kref.GAMMA_GAIT, g=kref.G)


def test_unlocked_phase_conserves_energy():
    """unlocked 相（膝曲げ可、3 DOF）は保存系（drift < 1e-7）。"""
    x0 = np.array([0.2, -0.2, -0.35, -1.0, -0.3, 0.4])
    sol = solve_ivp(lambda t, x: dynamics_unlocked(t, x, P), (0.0, 0.3), x0, rtol=1e-11, atol=1e-13)
    e0 = energy(sol.y[:, 0], P)
    drift = max(abs(energy(sol.y[:, k], P) - e0) for k in range(sol.y.shape[1]))
    assert drift < 1e-7 * abs(e0)


def test_locked_phase_conserves_energy():
    """locked 相（膝ロック、θ_th=θ_sh）も保存系。"""
    th = 0.2
    x0 = np.array([th, -0.25, -0.25, -1.0, -0.3, -0.3])
    sol = solve_ivp(lambda t, x: dynamics_locked(t, x, P), (0.0, 0.3), x0, rtol=1e-11, atol=1e-13)
    e0 = energy(sol.y[:, 0], P)
    drift = max(abs(energy(sol.y[:, k], P) - e0) for k in range(sol.y.shape[1]))
    assert drift < 1e-7 * abs(e0)
```

- [ ] **Step 2: FAIL 確認**: `uv run pytest tests/test_rocker_kneed_dynamics.py -v`（ModuleNotFoundError）

- [ ] **Step 3: rocker_kneed.py を実装（kneed.py の _build を 5 箇所変更）**

```python
# src/crane/models/rocker_kneed.py
"""Rocker-foot kneed walker（McGeer 1990b 原機械）。derive レイヤーで記号導出し lambdify。

Phase 3 の点足 kneed (kneed.py) に「stance 足の転がり」だけを足したモデル。
状態 x = [θ_st, θ_th, θ_sh, θ̇_st, θ̇_th, θ̇_sh]、4相機械・質量モデル・knee-strike は
kneed.py と同一。差分は 5 箇所のみ:
  1. hip = C_st − (L−R)·down(θ_st), C_st = [−R·θ_st, R]（stance 円弧足が転がる）
  2. stance 接触点 P_st = [−R·θ_st, 0]
  3. swing 円弧足接触 P_sw = [C_sw_x, 0], C_sw = knee_sw + (l_s−R)·down(θ_sh)
  4. 衝突 pivot: knee-strike/heel-strike の系全体 pivot を [0,0]→P_st, foot_sw→P_sw
  5. パラメータ R
R→0 で kneed.py（点足 kneed）に厳密縮退。strike 面 θ_st+θ_sw=0（両足同半径の対称性）。
"""

from dataclasses import dataclass
from functools import lru_cache

import numpy as np
import sympy as sp

from crane.derive.impact import angular_momentum
from crane.derive.lagrange import derive_qdd
from crane.model import HybridModel, PhaseSpec


@dataclass(frozen=True)
class RockerKneedParams:
    m_h: float  # hip 質量 [kg]
    m_t: float  # 大腿質量 [kg]
    m_s: float  # 脛質量 [kg]
    l_t: float  # 大腿長 hip→knee [m]
    l_s: float  # 脛長 knee→foot [m]
    b_t: float  # hip→大腿質量点 [m]
    b_s: float  # knee→脛質量点 [m]
    R: float  # 足半径 [m]
    gamma: float  # 斜面角 [rad]
    g: float = 9.81

    @property
    def l(self) -> float:  # noqa: E743
        return self.l_t + self.l_s


@lru_cache(maxsize=1)
def _build():
    th_st, th_th, th_sh = sp.symbols("th_st th_th th_sh")
    w_st, w_th, w_sh = sp.symbols("w_st w_th w_sh")
    m_h, m_t, m_s = sp.symbols("m_h m_t m_s", positive=True)
    l_t, l_s, b_t, b_s, R = sp.symbols("l_t l_s b_t b_s R", positive=True)
    gamma, g = sp.symbols("gamma g", positive=True)
    length = l_t + l_s

    def down(theta):
        return sp.Matrix([sp.sin(theta), -sp.cos(theta)])

    # (1) stance 脚: 円弧足が転がる。曲率中心 C_st 高さ R、hip は C_st から脚上向き (L−R)
    C_st = sp.Matrix([-R * th_st, R])
    hip = C_st - (length - R) * down(th_st)
    knee_st = hip + l_t * down(th_st)
    p_th_st = hip + b_t * down(th_st)
    p_sh_st = knee_st + b_s * down(th_st)
    # swing 脚
    p_th_sw = hip + b_t * down(th_th)
    knee_sw = hip + l_t * down(th_th)
    p_sh_sw = knee_sw + b_s * down(th_sh)
    # (2)(3) 転がり接触点
    P_st = sp.Matrix([-R * th_st, 0])
    C_sw = knee_sw + (l_s - R) * down(th_sh)  # swing 円弧足の曲率中心
    P_sw = sp.Matrix([C_sw[0], 0])

    q3, qd3 = [th_st, th_th, th_sh], [w_st, w_th, w_sh]
    bodies = [(m_h, hip), (m_t, p_th_st), (m_s, p_sh_st), (m_t, p_th_sw), (m_s, p_sh_sw)]

    def vel(r, q, qd):
        return r.jacobian(q) * sp.Matrix(qd)

    T3 = sum(mass * vel(r, q3, qd3).dot(vel(r, q3, qd3)) for mass, r in bodies) / 2
    g_dir = sp.Matrix([sp.sin(gamma), -sp.cos(gamma)])
    V = -g * sum(mass * r.dot(g_dir) for mass, r in bodies)

    params = (m_h, m_t, m_s, l_t, l_s, b_t, b_s, R, gamma, g)
    args6 = (th_st, th_th, th_sh, w_st, w_th, w_sh, *params)

    qdd3 = derive_qdd(q3, qd3, T3, V, simplify=False)
    f_qdd3 = sp.lambdify(args6, [qdd3[0], qdd3[1], qdd3[2]], "numpy")
    f_energy = sp.lambdify(args6, T3 + V, "numpy")
    f_kinetic = sp.lambdify(args6, T3, "numpy")

    th_sw, w_sw = sp.symbols("th_sw w_sw")
    lock = {th_th: th_sw, th_sh: th_sw, w_th: w_sw, w_sh: w_sw}
    T2 = T3.subs(lock, simultaneous=True)
    V2 = V.subs(lock, simultaneous=True)
    qdd2 = derive_qdd([th_st, th_sw], [w_st, w_sw], T2, V2, simplify=False)
    f_qdd2 = sp.lambdify((th_st, th_sw, w_st, w_sw, *params), [qdd2[0], qdd2[1]], "numpy")

    # --- knee-strike（脚交換なし、3→2 速度）: (4) 系全体 pivot を P_st に ---
    L_sys_pivot3 = angular_momentum(bodies, q3, qd3, P_st)
    L_swleg_hip3 = angular_momentum([(m_t, p_th_sw), (m_s, p_sh_sw)], q3, qd3, hip)
    wp_st, wp_sw = sp.symbols("wp_st wp_sw")
    post_ks = {w_st: wp_st, w_th: wp_sw, w_sh: wp_sw}
    eqs_ks = [
        L_sys_pivot3.subs(post_ks, simultaneous=True) - L_sys_pivot3,
        L_swleg_hip3.subs(post_ks, simultaneous=True) - L_swleg_hip3,
    ]
    A_ks, rhs_ks = sp.linear_eq_to_matrix(eqs_ks, [wp_st, wp_sw])
    qd_post_ks = A_ks.LUsolve(rhs_ks)
    f_impact_ks = sp.lambdify(args6, [qd_post_ks[0], qd_post_ks[1]], "numpy")

    # --- heel-strike（脚交換、両脚剛体、2→2 速度）: (4) pivot を P_st / P_sw に ---
    wq_st, wq_leg = sp.symbols("wq_st wq_leg")
    swap_hs = {
        th_st: th_th, th_th: th_st, th_sh: th_st,
        w_st: wq_st, w_th: wq_leg, w_sh: wq_leg,
    }
    L_stleg_hip3 = angular_momentum([(m_t, p_th_st), (m_s, p_sh_st)], q3, qd3, hip)
    L_sys_swfoot3 = angular_momentum(bodies, q3, qd3, P_sw)
    eqs_hs = [
        L_sys_pivot3.subs(swap_hs, simultaneous=True) - L_sys_swfoot3,
        L_swleg_hip3.subs(swap_hs, simultaneous=True) - L_stleg_hip3,
    ]
    A_hs, rhs_hs = sp.linear_eq_to_matrix(eqs_hs, [wq_st, wq_leg])
    qd_post_hs = A_hs.LUsolve(rhs_hs)
    f_impact_hs = sp.lambdify(args6, [qd_post_hs[0], qd_post_hs[1]], "numpy")

    return f_qdd3, f_qdd2, f_energy, f_kinetic, f_impact_ks, f_impact_hs


def _args(x, p: RockerKneedParams):
    return (*x, p.m_h, p.m_t, p.m_s, p.l_t, p.l_s, p.b_t, p.b_s, p.R, p.gamma, p.g)


def dynamics_unlocked(t: float, x, p: RockerKneedParams):
    f_qdd3 = _build()[0]
    qdd = f_qdd3(*_args(x, p))
    return [x[3], x[4], x[5], qdd[0], qdd[1], qdd[2]]


def dynamics_locked(t: float, x, p: RockerKneedParams):
    f_qdd2 = _build()[1]
    qdd = f_qdd2(
        x[0], x[1], x[3], x[4], p.m_h, p.m_t, p.m_s, p.l_t, p.l_s, p.b_t, p.b_s, p.R, p.gamma, p.g
    )
    return [x[3], x[4], x[5], qdd[0], qdd[1], qdd[1]]


def energy(x, p: RockerKneedParams) -> float:
    return float(_build()[2](*_args(x, p)))


def kinetic_energy(x, p: RockerKneedParams) -> float:
    return float(_build()[3](*_args(x, p)))
```

- [ ] **Step 4: テスト pass + 全体 + ruff(format & check) + Commit**

Run: `uv run pytest tests/test_rocker_kneed_dynamics.py -v`（2 PASSED）、`uv run pytest -q`、ruff。
**エネルギー保存が落ちたら**転がり項（C_st/hip）か down 符号を疑う。**許容は緩めない**。

```bash
git add src/crane/models/rocker_kneed.py tests/test_rocker_kneed_dynamics.py
git commit -m "feat: rocker-foot kneed rolling dynamics (unlocked + locked) via derive layer"
```

---

### Task 3: 衝突写像（knee-strike / heel-strike）+ R→0 が kneed に一致（TDD）

**Files:** Modify `src/crane/models/rocker_kneed.py`（公開 `kneestrike_map`, `heelstrike_map` 追加）; Create `tests/test_rocker_kneed_impact.py`

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_rocker_kneed_impact.py
import numpy as np

from crane import references_kneed as kref
from crane.models.kneed import KneedParams
from crane.models.kneed import dynamics_unlocked as kn_unlocked
from crane.models.kneed import dynamics_locked as kn_locked
from crane.models.kneed import kneestrike_map as kn_ks
from crane.models.kneed import heelstrike_map as kn_hs
from crane.models.rocker_kneed import (
    RockerKneedParams, dynamics_unlocked, dynamics_locked, heelstrike_map, kneestrike_map,
    kinetic_energy,
)

KW = dict(m_h=kref.M_HIP, m_t=kref.M_THIGH, m_s=kref.M_SHANK, l_t=kref.L_THIGH,
          l_s=kref.L_SHANK, b_t=kref.B_THIGH, b_s=kref.B_SHANK, gamma=kref.GAMMA_GAIT, g=kref.G)
P = RockerKneedParams(R=0.2, **KW)


def test_kneestrike_locks_and_dissipates():
    x_pre = np.array([0.1, -0.2, -0.35, -1.0, -0.3, 0.5])  # θ_sh<θ_th（膝曲げ）
    x_post = kneestrike_map(x_pre, P)
    assert np.allclose(x_post[:3], x_pre[:3])
    assert x_post[4] == x_post[5]  # 膝ロック
    assert kinetic_energy(x_post, P) < kinetic_energy(x_pre, P)


def test_heelstrike_swaps_and_dissipates():
    th = -0.18
    x_pre = np.array([th, -th, -th, -1.3, -0.5, -0.5])  # locked, strike 面
    x_post = heelstrike_map(x_pre, P)
    assert x_post[0] == -th and x_post[1] == th and x_post[2] == th
    assert x_post[4] == x_post[5]
    assert kinetic_energy(x_post, P) < kinetic_energy(x_pre, P)


def test_rocker_kneed_reduces_to_pointfoot_kneed():
    """R→0 で力学（両相）・衝突（knee/heel）が点足 kneed に一致。"""
    Pd = RockerKneedParams(R=1e-9, **KW)
    Pc = KneedParams(**KW)
    rng = np.random.default_rng(0)
    for _ in range(6):
        x = np.array([*rng.uniform(-0.4, 0.4, 3), *rng.uniform(-1.5, 1.5, 3)])
        du, ku = dynamics_unlocked(0.0, x, Pd), kn_unlocked(0.0, x, Pc)
        assert np.allclose(du[3:], ku[3:], rtol=0, atol=1e-4)
        xl = np.array([x[0], x[1], x[1], x[3], x[4], x[4]])  # locked: θ_th=θ_sh
        dl, kl = dynamics_locked(0.0, xl, Pd), kn_locked(0.0, xl, Pc)
        assert np.allclose(dl[3:], kl[3:], rtol=0, atol=1e-4)
        assert np.allclose(kneestrike_map(x, Pd), kn_ks(x, Pc), rtol=0, atol=1e-4)
    for _ in range(6):
        th = rng.uniform(-0.3, -0.1)
        x = np.array([th, -th, -th, *rng.uniform(-1.5, -0.2, 3)])
        assert np.allclose(heelstrike_map(x, Pd), kn_hs(x, Pc), rtol=0, atol=1e-4)
```

- [ ] **Step 2: FAIL 確認**: `uv run pytest tests/test_rocker_kneed_impact.py -v`（ImportError）

- [ ] **Step 3: 公開写像を追加（kneed.py と同一）**

```python
def kneestrike_map(x: np.ndarray, p: RockerKneedParams) -> np.ndarray:
    """knee-strike: 脚交換なし。swing 膝ロックで 3→2 速度。"""
    wp = _build()[4](*_args(x, p))
    return np.array([x[0], x[1], x[2], float(wp[0]), float(wp[1]), float(wp[1])])


def heelstrike_map(x: np.ndarray, p: RockerKneedParams) -> np.ndarray:
    """heel-strike: 脚交換。衝突中は両脚剛体。post で θ̇_th⁺=θ̇_sh⁺。"""
    wq = _build()[5](*_args(x, p))
    return np.array([x[1], x[0], x[0], float(wq[0]), float(wq[1]), float(wq[1])])
```

- [ ] **Step 4: テスト pass 確認**

Run: `uv run pytest tests/test_rocker_kneed_impact.py -v`（3 PASSED）
**`test_rocker_kneed_reduces_to_pointfoot_kneed` が落ちたら**: 転がり項の符号（C_st/P_st）、
heel-strike pivot（P_st vs P_sw）、swing 接触 C_sw の (l_s−R) を kneed.py と項ごとに比較。
**勝手に許容値を緩めない**。バグ修正は本 Task のスコープ内。

- [ ] **Step 5: 全体 + ruff(format & check) + Commit**

```bash
git add src/crane/models/rocker_kneed.py tests/test_rocker_kneed_impact.py
git commit -m "feat: rocker kneed knee/heel-strike (reduces to point-foot kneed at R->0)"
```

---

### Task 4: make_rocker_kneed + end-to-end stride（4相、TDD）

**Files:** Modify `src/crane/models/rocker_kneed.py`; Create `tests/test_rocker_kneed_stride.py`

- [ ] **Step 1: make_rocker_kneed を実装（kneed.py の make_kneed と同一）**

```python
def make_rocker_kneed(p: RockerKneedParams) -> HybridModel:
    """相機械: unlocked → knee-strike → locked → heel-strike（kneed と同一構造）。"""
    unlocked = PhaseSpec(
        dynamics=lambda t, x: dynamics_unlocked(t, x, p),
        event_value=lambda x: x[2] - x[1],  # θ_sh − θ_th = 0 で knee-strike
        event_accept=lambda x: True,
        impact=lambda x: kneestrike_map(x, p),
    )
    locked = PhaseSpec(
        dynamics=lambda t, x: dynamics_locked(t, x, p),
        event_value=lambda x: x[0] + x[1],  # θ_st + θ_sw = 0 で heel-strike
        event_accept=lambda x: x[0] < 0.0 and (x[3] + x[4]) < 0.0,
        impact=lambda x: heelstrike_map(x, p),
    )
    return HybridModel(
        phases=(unlocked, locked),
        lift=lambda y: np.array([y[0], -y[0], -y[0], y[1], y[2], y[2]]),
        project=lambda x: np.array([x[0], x[3], x[4]]),
    )
```

- [ ] **Step 2: end-to-end テストを書く**

```python
# tests/test_rocker_kneed_stride.py
import numpy as np

from crane import references_kneed as kref
from crane.models.rocker_kneed import RockerKneedParams, make_rocker_kneed
from crane.stride import stride

P = RockerKneedParams(m_h=kref.M_HIP, m_t=kref.M_THIGH, m_s=kref.M_SHANK, l_t=kref.L_THIGH,
                      l_s=kref.L_SHANK, b_t=kref.B_THIGH, b_s=kref.B_SHANK,
                      R=0.2, gamma=kref.GAMMA_GAIT, g=kref.G)
MODEL = make_rocker_kneed(P)


def test_stride_passes_both_phases_and_returns_to_section():
    """seed から unlocked→knee-strike→locked→heel-strike を経て断面に戻る。"""
    result = stride(MODEL, MODEL.lift(np.array(kref.SECTION_GUESS)))
    x_end = result.x_end
    assert np.isclose(x_end[1], -x_end[0], atol=1e-8)  # θ_th = −θ_st
    assert np.isclose(x_end[2], -x_end[0], atol=1e-8)  # θ_sh = −θ_st
    assert result.t_step > 0.1
    flexion = result.x[2] - result.x[1]  # θ_sh − θ_th
    assert np.max(np.abs(flexion)) > 1e-3  # 膝屈曲が現れる
```

- [ ] **Step 3: 実行・デバッグ**: `uv run pytest tests/test_rocker_kneed_stride.py -v`

StrideError なら相軌跡を print し (a) 即時 strike (b) 転倒 (c) イベント未検出 を切り分け。
seed（kref.SECTION_GUESS は点足 kneed 用）が R=0.2 で転倒するなら、R を小さく（例 0.05）するか
swing 速度を調整した seed を使い、変更理由を報告（**物理は変えない**）。

- [ ] **Step 4: 全体 + ruff + Commit**

```bash
git add src/crane/models/rocker_kneed.py tests/test_rocker_kneed_stride.py
git commit -m "feat: rocker kneed phase machine and end-to-end stride"
```

---

### Task 5: R→0 退化ゲート — Phase 3 点足 kneed と一致（主軸・最強の検証）

**Files:** Create `tests/test_rocker_kneed_pointfoot_limit.py`

- [ ] **Step 1: テストを書く**

```python
# tests/test_rocker_kneed_pointfoot_limit.py
import numpy as np

from crane import references_kneed as kref
from crane.models.kneed import KneedParams, make_kneed
from crane.models.rocker_kneed import RockerKneedParams, make_rocker_kneed
from crane.search import find_limit_cycle

KW = dict(m_h=kref.M_HIP, m_t=kref.M_THIGH, m_s=kref.M_SHANK, l_t=kref.L_THIGH,
          l_s=kref.L_SHANK, b_t=kref.B_THIGH, b_s=kref.B_SHANK, gamma=kref.GAMMA_GAIT, g=kref.G)


def test_full_cycle_reduces_to_pointfoot_kneed():
    """R→0 の全 stride 不動点・固有値が Phase 3 検証済み点足 kneed に一致。"""
    fp_k = find_limit_cycle(make_kneed(KneedParams(**KW)), np.array(kref.SECTION_GUESS))
    assert fp_k.converged
    fp_r = find_limit_cycle(make_rocker_kneed(RockerKneedParams(R=1e-9, **KW)), fp_k.y.copy())
    assert fp_r.converged
    assert np.allclose(fp_r.y, fp_k.y, atol=1e-5)
    mags_r = np.sort(np.abs(fp_r.eigenvalues))[::-1]
    mags_k = np.sort(np.abs(fp_k.eigenvalues))[::-1]
    assert np.allclose(mags_r, mags_k, atol=2e-3)
```

- [ ] **Step 2: 実行**: `uv run pytest tests/test_rocker_kneed_pointfoot_limit.py -v`（1 PASSED）
Phase 3 不動点は約 (0.23859, −1.10959, −0.05715)。**FAIL 時**は Task 3 の R→0 写像一致に戻る。
**許容は緩めない**。

- [ ] **Step 3: 全体 + ruff + Commit**

```bash
git add tests/test_rocker_kneed_pointfoot_limit.py
git commit -m "test: rocker kneed degenerates to verified point-foot kneed (R->0 gate)"
```

---

### Task 6: McGeer 1990b リミットサイクル + 文献ゲート

**Files:** Create `tests/test_rocker_kneed_cycle.py`

- [ ] **Step 1: ゲートテストを書く**

```python
# tests/test_rocker_kneed_cycle.py
import numpy as np

from crane import references_mcgeer_knees as ref
from crane.models.rocker_kneed import RockerKneedParams, make_rocker_kneed
from crane.search import find_limit_cycle
from crane.stride import stride

P = RockerKneedParams(m_h=ref.M_HIP, m_t=ref.M_THIGH, m_s=ref.M_SHANK, l_t=ref.L_THIGH,
                      l_s=ref.L_SHANK, b_t=ref.B_THIGH, b_s=ref.B_SHANK,
                      R=ref.R_FOOT, gamma=ref.GAMMA_GAIT, g=ref.G)
MODEL = make_rocker_kneed(P)


def test_rocker_kneed_limit_cycle_exists():
    fp = find_limit_cycle(MODEL, np.array(ref.SECTION_GUESS))
    assert fp.converged
    if ref.PUBLISHED_STABLE:
        assert np.max(np.abs(fp.eigenvalues)) < 1.0


def test_rocker_kneed_gait_matches_published():
    fp = find_limit_cycle(MODEL, np.array(ref.SECTION_GUESS))
    assert fp.converged
    result = stride(MODEL, MODEL.lift(fp.y))
    if ref.STEP_PERIOD is not None:
        # McGeer の period が無次元 √(l/g) 単位なら換算（references の単位コメントを確認）
        assert np.isclose(result.t_step, ref.STEP_PERIOD, rtol=ref.GAIT_TOLERANCE)
    if ref.POINCARE_EIGENVALUES is not None:
        ours = np.sort(np.abs(fp.eigenvalues))
        theirs = np.sort(np.abs(np.array(ref.POINCARE_EIGENVALUES)))
        assert np.allclose(ours, theirs, rtol=ref.GAIT_TOLERANCE)
```

- [ ] **Step 2: 実行・探索**

収束しない場合（決定論的、物理は変えない）:
1. R=1e-9（= Phase 3 不動点近傍）から McGeer の R まで R-continuation で seed 供給
2. SECTION_GUESS 近傍の粗グリッドで ‖S(y)−y‖ 最小点を seed に
3. 失敗モードを軌跡付きで報告
不動点・固有値・step period を必ず報告。SECTION_GUESS を補正したら references に根拠コメント付き更新。
**STEP_PERIOD の単位**（秒 vs √(l/g)）を references のコメントで確認し、必要なら換算（Phase 3.5 の教訓）。

- [ ] **Step 3: Commit**

```bash
git add tests/test_rocker_kneed_cycle.py src/crane/references_mcgeer_knees.py
git commit -m "feat: rocker kneed limit cycle gated on McGeer 1990b"
```

---

### Task 7: viz（4セグメント + 円弧足）+ walk_rocker_kneed.py デモ

**Files:** Modify `src/crane/viz.py`（`rocker_kneed_joints` + `animate_rocker_kneed`）; Create `scripts/walk_rocker_kneed.py`, `tests/test_viz_rocker_kneed.py`

既存 `kneed_joints`/`animate_kneed`（4セグメント）と `rocker_joints`/`animate_rocker`（円弧足の
転がり）を合成する。stance は P_st（転がる接触）→C_st→hip、swing は hip→knee_sw→C_sw（円弧足）。

- [ ] **Step 1: 幾何テストを書く**

```python
# tests/test_viz_rocker_kneed.py
import numpy as np
from crane.viz import rocker_kneed_joints


def test_stance_contact_at_ground_and_C_height_R():
    # pts = (stance_contact, stance_C, knee_st, hip, knee_sw, swing_C, swing_lowest)
    pts = rocker_kneed_joints(np.array([0.2, -0.2, -0.2, 0, 0, 0]),
                              l_t=0.5, l_s=0.5, R=0.3, contact_x=0.0)
    assert np.isclose(pts[0][1], 0.0, atol=1e-12)   # 接触点 y=0
    assert np.isclose(pts[1][1], 0.3, atol=1e-12)   # 曲率中心 C 高さ R
```

- [ ] **Step 2: viz.py に実装**

```python
def rocker_kneed_joints(x: np.ndarray, l_t: float, l_s: float, R: float, contact_x: float):
    """rocker kneed の関節座標（slope frame）。stance 接触点を (contact_x, 0) に。

    返り値: (stance_contact, stance_C, knee_st, hip, knee_sw, swing_C, swing_lowest)
    """
    th_st, th_th, th_sh = x[0], x[1], x[2]
    length = l_t + l_s

    def down(theta):
        return np.array([np.sin(theta), -np.cos(theta)])

    C_st = np.array([contact_x, R])
    hip = C_st - (length - R) * down(th_st)
    knee_st = hip + l_t * down(th_st)
    knee_sw = hip + l_t * down(th_th)
    C_sw = knee_sw + (l_s - R) * down(th_sh)
    swing_lowest = C_sw + np.array([0.0, -R])
    return np.array([contact_x, 0.0]), C_st, knee_st, hip, knee_sw, C_sw, swing_lowest
```

`animate_rocker_kneed(strides, l_t, l_s, R, gamma, out, fps=30)`: `animate_rocker`（Phase 3.5）の
転がり込み（stance contact を frame ごとに `contact_x += -R*(th_st[i]-th_st[i-1])` で転がし、
heel-strike で swing 接触 x にアンカー前進）と `animate_kneed` の4セグメント描画を合成。
stance を P_st→C_st（円弧足を `matplotlib.patches.Arc` か polyline）→knee_st→hip、swing を
hip→knee_sw→C_sw（円弧足）で描く。全体 −γ 回転、ffmpeg→gif フォールバック。

- [ ] **Step 3: walk_rocker_kneed.py を書く**（walk_rocker.py + walk_kneed.py の合成）

argparse（--gamma-deg default=rad2deg(GAMMA_GAIT), --strides 30, --perturb 0.005）、
references_mcgeer_knees から params 構築、find_limit_cycle(SECTION_GUESS) + 収束ガード +
不安定警告、摂動多歩ループ（deviation = ‖project(x_end)−fp.y‖、膝屈曲 max も print）、
plot_phase_portrait、animate_rocker_kneed、meta.json（params, fixed_point, eigenvalues_abs,
stable, n_strides_completed, R, knee_flexion_max_deg, perturb）。

- [ ] **Step 4: 実行確認 + 全体 + ruff + Commit**

Run: `uv run python scripts/walk_rocker_kneed.py` → mp4/png/meta 生成、deviation 減衰、
円弧足の転がり＋膝屈曲が見えること。

```bash
git add src/crane/viz.py tests/test_viz_rocker_kneed.py scripts/walk_rocker_kneed.py
git commit -m "feat: rocker kneed walk demo with arc-foot + knee animation"
```

---

### Task 8: ドキュメント更新 + 歩容判定ゲート

**Files:** Modify `GOALS.md`, `README.md`

- [ ] **Step 1: GOALS.md に Phase 3.6 節を実績で記入**（Phase 1-3.5 と同じ形式: チェックリスト・
  知見・今後の課題）。最後に未チェックで:
  `- [ ] **ぽんぽこ殿の歩容判定**: walk.mp4 が「円弧足で転がりつつ膝を使って歩く」ように見える`

- [ ] **Step 2: README.md に Phase 3.6 結果 + walk_rocker_kneed.py の使い方 + テスト数更新 + Commit**

```bash
git add GOALS.md README.md
git commit -m "docs: record phase 3.6 results and gait judgment gate"
```

- [ ] **Step 3: 歩容判定ゲート（人間ゲート — STOP）**

`data/runs/<ts>_rocker_kneed_*/walk.mp4` をぽんぽこ殿に提示。判定が出るまで完了としない。

---

## Self-Review Notes

- 設計書（`docs/2026-06-16-rocker-foot-kneed-design.md`）の要件をカバー: スコープ＆ゲート(Task 1,5,6,8) /
  検証アンカー(Task 1,5,6) / 物理モデル＝kneed+転がり(Task 2,3) / コード構成(全 Task) /
  三段防御（不変量 Task 2-4 → R→0 退化ゲート Task 5 → McGeer 文献 Task 6 → 歩容 Task 8）。
- **最大リスク = 転がりが両相＋両衝突に正しく入るか**。対策: Task 3 で R→0 を unlocked/locked/
  knee/heel の 4 経路すべてで個別検証 → Task 5 で全 stride 退化ゲート。符号/pivot ミスはここで捕捉。
- derive 層・kneed.py・rocker_compass.py は無改変。kneed.py の _build を 5 箇所変更した自前 _build。
- 型一貫性: `RockerKneedParams(m_h,m_t,m_s,l_t,l_s,b_t,b_s,R,gamma,g)`、`_args` 順序（R は b_s と
  gamma の間）、`dynamics_unlocked/dynamics_locked/energy/kinetic_energy/kneestrike_map/
  heelstrike_map/make_rocker_kneed` を Task 2-4 で統一。
- 文献数値はプランに書かない。R→0 退化の照合先は Phase 3 実測 (0.23859, −1.10959, −0.05715)。
- McGeer 1990b が薄ければ Task 6 は存在＋安定性＋R→0 退化に縮退（GAIT_TOLERANCE/None で吸収）。
- STEP_PERIOD の単位（秒 vs √(l/g)）に注意（Phase 3.5 の教訓を Task 6 に明記）。
