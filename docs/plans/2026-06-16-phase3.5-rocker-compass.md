# Crane Phase 3.5: Rocker-foot Compass Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 円弧足（半径 R）を持つ 2D 直脚 compass 歩行器を derive レイヤーで記号導出し、リミットサイクルを発見・安定性を固有値で証明し、McGeer 1990a と照合する。

**Architecture:** 既存 compass を汚さず新モデル `models/rocker_compass.py` を追加。各脚を**2 質点 (m/2 ずつ) で表現**して既存の derive レイヤー（point-mass の `angular_momentum`/`derive_qdd`）を無改変で再利用。stance 足は円弧で転がり（曲率中心 `C_x=−R·θ_st`）、R→0 で Phase 2 検証済み点足 compass に厳密縮退する。これが最強の検証ゲート。

**Tech Stack:** Python 3.12 / uv / numpy / scipy / sympy / matplotlib / pytest / ruff

**Spec:** `docs/2026-06-16-rocker-foot-compass-design.md`

---

## 物理モデル早見表（rocker-foot compass）

- 状態 `x = [θ_st, θ_sw, θ̇_st, θ̇_sw]`（slope 法線基準の絶対角、進行 +x）。compass と同型。
- 断面 `y = (θ_st, θ̇_st, θ̇_sw)`（heel-strike 直後）。lift/project は compass と同一。
- **脚 = 一般剛体**: 脚質量 m、hip→CoM 距離 c、CoM 回りの回転半径 ρ。これを **hip から距離
  (c−ρ) と (c+ρ) の 2 質点 (m/2 ずつ)** で等価表現する（総質量 m・CoM 位置 c・慣性 mρ² を再現）。
  ρ→0 で単一質点に縮退。hip 質量 m_h。脚長 L（hip→直立時の接地点）。足半径 R。斜面 γ。
- **転がり接触**: stance 円弧足の曲率中心 C は地面から高さ R。転がりなし条件 v_C = θ̇_st ẑ×(C−P)
  から `C = [−R·θ_st, R]`（x0=0 は定数で EOM に効かない）。`hip = C − (L−R)·down(θ_st)`。
  - 検算（R→0）: `hip = [−L sinθ_st, L cosθ_st]` = compass.py の hip 式に一致。
- **strike 条件**: 両足とも半径 R。swing 足曲率中心 `C_sw = hip + (L−R)·down(θ_sw)` が高さ R に
  達した瞬間。展開すると `(L−R)(cosθ_st − cosθ_sw)=0` ⇒ 非自明解 `θ_sw = −θ_st`、すなわち
  `g(x)=θ_st+θ_sw=0`（**compass と厳密に同一**）。受理は compass と同じ（θ_st<0 かつ下降接地）。
- **衝突**: 接触点回りの角運動量保存。新接触点 = swing 足接触点 `P_sw=[C_sw_x, 0]`。
  stance 接触点 `P_st=[−R·θ_st, 0]`。compass と同じ swap 構造（pre/post の同形式を脚ラベル
  交換で評価）。R→0,ρ→0 で compass の衝突写像に一致（degenerate gate で検証）。
- **退化ゲート**: `L=1, c=0.5, ρ=1e-9, R=1e-9, m=5, m_h=10` で Goswami compass
  (a=b=0.5) と全 stride 不動点 (0.27103, −1.09238, −0.37737)・固有値が一致するはず。
- 文献値（McGeer 1990a §5）は Task 1 で provenance 付き取得。**プランに数値は書かない**。

## File Structure

| パス | 責務 |
|---|---|
| `src/crane/references_mcgeer.py` | McGeer 公表値（provenance、パラメータ変換、固有値、step period） |
| `src/crane/models/rocker_compass.py` | rocker compass（derive レイヤー、新規） |
| `src/crane/viz.py` | `rocker_joints` + `animate_rocker`（円弧足の弧描画、転がり） |
| `scripts/walk_rocker.py` | Phase 3.5 デモ |
| `tests/test_references_mcgeer.py` 他 | 各 Task のゲート |

---

### Task 1: McGeer 1990a の文献調査（references_mcgeer.py）

**Files:**
- Create: `src/crane/references_mcgeer.py`
- Test: `tests/test_references_mcgeer.py`

**Web 調査タスク（Phase 3 Task 2 と同じ規律）。** 一次資料は McGeer (1990) "Passive Dynamic
Walking", IJRR 9(2):62–82（Cornell/Ruina ラボ公開 PDF。スキャンのためページ画像から読取）。

取得するもの（早見表の規約 m, m_h, c, ρ, L, R, γ へ変換し、変換根拠を docstring に記録）:
- §5「Steady Walking of a General 2D Biped」の**直脚円弧足モデル**のパラメータ
  （foot radius R、leg radius of gyration r_gyr、mass center 位置、hip 質量、脚長 L、斜面 γ）
- gait の公表数値（不動点・step period・固有値）。本文印刷値と図読取値を区別し、図のみは
  digitized ±X% で正直ラベル。§8/Table 1 の固有値（速度/swing/totter ≈ 0.70/−0.65/−0.43）は
  **test machine（膝あり機）の可能性**があるため、直脚モデルの値か必ず確認。直脚モデルの
  印刷固有値が無ければその旨を記録し、固有値は参考比較に縮退（存在＋安定性＋R→0 退化を主ゲート）。

- [ ] **Step 1: ゲートテストを書き FAIL 確認**

```python
# tests/test_references_mcgeer.py
from crane import references_mcgeer as ref


def test_mcgeer_references_are_filled():
    assert "http" in ref.PROVENANCE
    for name in ["M_LEG", "M_HIP", "C_HIP_TO_COM", "RHO_GYR", "L_LEG", "R_FOOT"]:
        assert isinstance(getattr(ref, name), float), name
    assert isinstance(ref.GAMMA_GAIT, float)
    assert 0.0 < ref.GAMMA_GAIT < 0.3
    assert len(ref.SECTION_GUESS) == 3  # (θ_st, θ̇_st, θ̇_sw)
    assert isinstance(ref.PUBLISHED_STABLE, bool)
```

Run: `uv run pytest tests/test_references_mcgeer.py -v` → FAIL（ModuleNotFoundError）

- [ ] **Step 2: Web 調査、references_mcgeer.py 記入**

必須フィールド: `PROVENANCE`, `M_LEG`, `M_HIP`, `C_HIP_TO_COM`, `RHO_GYR`, `L_LEG`,
`R_FOOT`, `G`, `GAMMA_GAIT`, `SECTION_GUESS`（3D、変換根拠コメント必須）, `GAIT_TOLERANCE`
（根拠コメント）, `STEP_PERIOD: float | None`, `POINCARE_EIGENVALUES: tuple | None`,
`PUBLISHED_STABLE: bool`。論文に無い量は None + 理由。

- [ ] **Step 3: テスト pass + ruff + Commit**

```bash
uv run pytest tests/test_references_mcgeer.py -v && uv run ruff format . && uv run ruff check .
git add src/crane/references_mcgeer.py tests/test_references_mcgeer.py
git commit -m "feat: record McGeer 1990a rocker-foot compass values with provenance"
```

（commit body に `Parameter conversion: <内容>` と固有値の素性判断を記録）

---

### Task 2: rocker_compass.py — 転がり相の力学（TDD）

**Files:**
- Create: `src/crane/models/rocker_compass.py`
- Test: `tests/test_rocker_dynamics.py`

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_rocker_dynamics.py
import numpy as np
from scipy.integrate import solve_ivp

from crane.models.rocker_compass import RockerCompassParams, dynamics, energy

P = RockerCompassParams(m=5.0, m_h=10.0, c=0.5, rho=0.15, L=1.0, R=0.3,
                        gamma=0.05, g=9.81)


def test_equilibrium_hanging_is_fixed():
    """両脚が重力方向 (絶対角 γ) に揃った静止で加速度ゼロ。"""
    x = np.array([P.gamma, P.gamma, 0.0, 0.0])
    d = dynamics(0.0, x, P)
    assert np.allclose([d[2], d[3]], [0.0, 0.0], atol=1e-9)


def test_rolling_phase_conserves_energy():
    """転がり相は保存系（エネルギー drift < 1e-7）。"""
    x0 = np.array([0.2, -0.25, -1.0, 0.3])
    sol = solve_ivp(lambda t, x: dynamics(t, x, P), (0.0, 0.4), x0,
                    rtol=1e-11, atol=1e-13)
    e0 = energy(sol.y[:, 0], P)
    drift = max(abs(energy(sol.y[:, k], P) - e0) for k in range(sol.y.shape[1]))
    assert drift < 1e-7 * abs(e0)
```

- [ ] **Step 2: FAIL 確認（ModuleNotFoundError）**

Run: `uv run pytest tests/test_rocker_dynamics.py -v`

- [ ] **Step 3: rocker_compass.py の力学部分を実装**

```python
# src/crane/models/rocker_compass.py
"""Rocker-foot（円弧足）2D compass。derive レイヤーで記号導出し lambdify。

状態 x = [θ_st, θ_sw, θ̇_st, θ̇_sw]（slope 法線基準の絶対角、進行 +x）。
各脚 = 一般剛体を「hip から距離 c±ρ の 2 質点 (m/2)」で等価表現（総質量 m・
CoM 位置 c・慣性 mρ² を再現。ρ→0 で質点に縮退）。stance 円弧足は曲率中心
C=[−R·θ_st, R] で転がる。hip = C − (L−R)·down(θ_st)。R→0 で点足 compass に縮退。

strike 面 g(x)=θ_st+θ_sw=0（両足同半径 R の幾何対称性から compass と同一）。
衝突は新接触点 P_sw 回りの角運動量保存 + 脚交換（compass と同じ swap 構造）。
"""

from dataclasses import dataclass
from functools import lru_cache

import numpy as np
import sympy as sp

from crane.derive.impact import angular_momentum
from crane.derive.lagrange import derive_qdd
from crane.model import HybridModel, PhaseSpec


@dataclass(frozen=True)
class RockerCompassParams:
    m: float       # 脚質量 [kg]
    m_h: float     # hip 質量 [kg]
    c: float       # hip → 脚 CoM 距離 [m]
    rho: float     # 脚 CoM 回りの回転半径 [m]
    L: float       # 脚長 hip→直立時接地点 [m]
    R: float       # 足半径 [m]
    gamma: float   # 斜面角 [rad]
    g: float = 9.81


@lru_cache(maxsize=1)
def _build():
    th_st, th_sw, w_st, w_sw = sp.symbols("th_st th_sw w_st w_sw")
    m, m_h, c, rho, L, R, gamma, g = sp.symbols("m m_h c rho L R gamma g", positive=True)
    q, qd = [th_st, th_sw], [w_st, w_sw]

    def down(theta):
        return sp.Matrix([sp.sin(theta), -sp.cos(theta)])

    # stance 円弧足: 曲率中心 C は高さ R、転がりで C_x = -R*th_st
    C_st = sp.Matrix([-R * th_st, R])
    hip = C_st - (L - R) * down(th_st)
    # 各脚 = 2 質点 (m/2): hip から距離 c-rho と c+rho
    p_st1 = hip + (c - rho) * down(th_st)
    p_st2 = hip + (c + rho) * down(th_st)
    p_sw1 = hip + (c - rho) * down(th_sw)
    p_sw2 = hip + (c + rho) * down(th_sw)
    C_sw = hip + (L - R) * down(th_sw)
    foot_sw_contact = sp.Matrix([C_sw[0], 0])      # strike 時の swing 接触点
    P_st = sp.Matrix([-R * th_st, 0])              # stance 接触点

    bodies = [
        (m_h, hip),
        (m / 2, p_st1), (m / 2, p_st2),
        (m / 2, p_sw1), (m / 2, p_sw2),
    ]

    def vel(r):
        return r.jacobian(q) * sp.Matrix(qd)

    T = sum(mass * vel(r).dot(vel(r)) for mass, r in bodies) / 2
    g_dir = sp.Matrix([sp.sin(gamma), -sp.cos(gamma)])
    V = -g * sum(mass * r.dot(g_dir) for mass, r in bodies)

    qdd = derive_qdd(q, qd, T, V, simplify=False)
    params = (m, m_h, c, rho, L, R, gamma, g)
    args = (th_st, th_sw, w_st, w_sw, *params)
    f_qdd = sp.lambdify(args, [qdd[0], qdd[1]], "numpy")
    f_energy = sp.lambdify(args, T + V, "numpy")
    f_kinetic = sp.lambdify(args, T, "numpy")

    # --- 衝突写像（compass と同じ swap 構造、pivot は接触点） ---
    st_masses = [(m / 2, p_st1), (m / 2, p_st2)]
    sw_masses = [(m / 2, p_sw1), (m / 2, p_sw2)]
    L_sys_stance = angular_momentum(bodies, q, qd, P_st)
    L_sys_swfoot = angular_momentum(bodies, q, qd, foot_sw_contact)
    L_st_hip = angular_momentum(st_masses, q, qd, hip)
    L_sw_hip = angular_momentum(sw_masses, q, qd, hip)

    wp_st, wp_sw = sp.symbols("wp_st wp_sw")
    swap = {th_st: th_sw, th_sw: th_st, w_st: wp_st, w_sw: wp_sw}
    eqs = [
        L_sys_stance.subs(swap, simultaneous=True) - L_sys_swfoot,
        L_sw_hip.subs(swap, simultaneous=True) - L_st_hip,
    ]
    A, rhs = sp.linear_eq_to_matrix(eqs, [wp_st, wp_sw])
    qd_post = A.LUsolve(rhs)
    f_impact = sp.lambdify(args, [qd_post[0], qd_post[1]], "numpy")

    return f_qdd, f_energy, f_kinetic, f_impact


def _args(x, p: RockerCompassParams):
    return (*x, p.m, p.m_h, p.c, p.rho, p.L, p.R, p.gamma, p.g)


def dynamics(t: float, x, p: RockerCompassParams):
    f_qdd = _build()[0]
    qdd = f_qdd(*_args(x, p))
    return [x[2], x[3], qdd[0], qdd[1]]


def energy(x, p: RockerCompassParams) -> float:
    return float(_build()[1](*_args(x, p)))


def kinetic_energy(x, p: RockerCompassParams) -> float:
    return float(_build()[2](*_args(x, p)))
```

- [ ] **Step 4: テスト pass 確認 + Commit**

Run: `uv run pytest tests/test_rocker_dynamics.py -v` → 2 PASSED、全体 green、ruff

```bash
git add src/crane/models/rocker_compass.py tests/test_rocker_dynamics.py
git commit -m "feat: rocker-foot compass rolling dynamics via derive layer"
```

---

### Task 3: 衝突写像 + R→0 が compass に一致（TDD）

**Files:**
- Modify: `src/crane/models/rocker_compass.py`（公開関数 `heelstrike_map` 追加）
- Test: `tests/test_rocker_impact.py`

- [ ] **Step 1: 失敗するテストを書く**

```python
# tests/test_rocker_impact.py
import numpy as np

from crane.models.compass import CompassParams, heelstrike_map as compass_strike
from crane.models.compass import dynamics as compass_dyn
from crane.models.rocker_compass import (
    RockerCompassParams, dynamics, heelstrike_map, kinetic_energy,
)

P = RockerCompassParams(m=5.0, m_h=10.0, c=0.5, rho=0.15, L=1.0, R=0.3,
                        gamma=0.05, g=9.81)


def test_heelstrike_swaps_and_dissipates():
    th = -0.18
    x_pre = np.array([th, -th, -1.4, -0.5])   # strike 面 θ_st+θ_sw=0 上
    x_post = heelstrike_map(x_pre, P)
    assert x_post[0] == -th          # 脚交換
    assert x_post[1] == th
    assert kinetic_energy(x_post, P) < kinetic_energy(x_pre, P)


def test_heelstrike_zero_velocity_is_fixed():
    th = -0.18
    x_pre = np.array([th, -th, 0.0, 0.0])
    x_post = heelstrike_map(x_pre, P)
    assert np.allclose(x_post[2:], np.zeros(2), atol=1e-12)


def test_rocker_reduces_to_compass_dynamics_and_impact():
    """R→0, ρ→0, c=b で rocker が点足 compass の力学・衝突に一致。"""
    Pd = RockerCompassParams(m=5.0, m_h=10.0, c=0.5, rho=1e-9, L=1.0, R=1e-9,
                             gamma=0.05, g=9.81)
    Pc = CompassParams(m=5.0, m_h=10.0, a=0.5, b=0.5, gamma=0.05, g=9.81)
    rng = np.random.default_rng(0)
    for _ in range(8):
        th_st, th_sw = rng.uniform(-0.4, 0.4, 2)
        w_st, w_sw = rng.uniform(-2.0, 2.0, 2)
        x = np.array([th_st, th_sw, w_st, w_sw])
        dr = dynamics(0.0, x, Pd)
        dc = compass_dyn(0.0, x, Pc)
        assert np.allclose([dr[2], dr[3]], [dc[2], dc[3]], rtol=0, atol=1e-4)
    for _ in range(8):
        th = rng.uniform(-0.3, -0.1)
        w_st, w_sw = rng.uniform(-2.0, -0.2, 2)
        x = np.array([th, -th, w_st, w_sw])
        gr = heelstrike_map(x, Pd)
        gc = compass_strike(x, Pc)
        assert np.allclose(gr, gc, rtol=0, atol=1e-4)
```

- [ ] **Step 2: FAIL 確認（ImportError: heelstrike_map）**

- [ ] **Step 3: heelstrike_map を追加**

```python
def heelstrike_map(x: np.ndarray, p: RockerCompassParams) -> np.ndarray:
    """衝突写像 + 脚ラベル交換。post 速度は post ラベルで返る。"""
    wp = _build()[3](*_args(x, p))
    return np.array([x[1], x[0], float(wp[0]), float(wp[1])])
```

- [ ] **Step 4: テスト pass 確認**

Run: `uv run pytest tests/test_rocker_impact.py -v` → 3 PASSED
**FAIL 時の切り分け**: `test_rocker_reduces_to_compass_*` が落ちたら転がり項の符号
（`C_st`/`P_st`）か swap pivot（`P_st` vs `foot_sw_contact`）を疑う。compass の式と
項ごとに比較。**勝手に許容値を緩めない**。

- [ ] **Step 5: Commit**

```bash
git add src/crane/models/rocker_compass.py tests/test_rocker_impact.py
git commit -m "feat: rocker compass heel-strike (reduces to point-foot compass at R->0)"
```

---

### Task 4: make_rocker_compass + end-to-end stride（TDD）

**Files:**
- Modify: `src/crane/models/rocker_compass.py`
- Test: `tests/test_rocker_stride.py`

- [ ] **Step 1: make_rocker_compass を実装**

```python
def make_rocker_compass(p: RockerCompassParams) -> HybridModel:
    return HybridModel(
        phases=(
            PhaseSpec(
                dynamics=lambda t, x: dynamics(t, x, p),
                event_value=lambda x: x[0] + x[1],
                event_accept=lambda x: x[0] < 0.0 and (x[2] + x[3]) < 0.0,
                impact=lambda x: heelstrike_map(x, p),
            ),
        ),
        lift=lambda y: np.array([y[0], -y[0], y[1], y[2]]),
        project=lambda x: np.array([x[0], x[2], x[3]]),
    )
```

- [ ] **Step 2: end-to-end テストを書く**

```python
# tests/test_rocker_stride.py
import numpy as np

from crane.models.rocker_compass import RockerCompassParams, make_rocker_compass
from crane.stride import stride

P = RockerCompassParams(m=5.0, m_h=10.0, c=0.5, rho=0.15, L=1.0, R=0.3,
                        gamma=0.05, g=9.81)
MODEL = make_rocker_compass(P)


def test_stride_returns_to_section():
    """素直な初期条件から 1 stride で断面（θ_sw=−θ_st）に戻る。"""
    x0 = MODEL.lift(np.array([0.2, -1.0, -0.3]))
    result = stride(MODEL, x0)
    assert np.isclose(result.x_end[1], -result.x_end[0], atol=1e-8)
    assert result.t_step > 0.1
```

- [ ] **Step 3: 実行・デバッグ**

Run: `uv run pytest tests/test_rocker_stride.py -v`
StrideError の場合: phase 軌跡を print し、(a) 即時 strike（受理条件）、(b) 転倒、
(c) イベント未検出 を切り分け。初期条件を変えて挙動を報告（**パラメータの物理は変えない**）。

- [ ] **Step 4: 全テスト + ruff + Commit**

```bash
git add src/crane/models/rocker_compass.py tests/test_rocker_stride.py
git commit -m "feat: rocker compass phase machine and end-to-end stride"
```

---

### Task 5: 退化ゲート — R→0, ρ→0 で Phase 2 compass と一致（最強の検証）

**Files:**
- Test: `tests/test_rocker_compass_limit.py`

- [ ] **Step 1: テストを書く**

```python
# tests/test_rocker_compass_limit.py
import numpy as np

from crane import references_goswami as gref
from crane.models.compass import CompassParams, make_compass
from crane.models.rocker_compass import RockerCompassParams, make_rocker_compass
from crane.search import find_limit_cycle

P_DEG = RockerCompassParams(m=gref.M_LEG, m_h=gref.M_HIP, c=gref.B, rho=1e-9,
                            L=gref.A + gref.B, R=1e-9, gamma=gref.GAMMA_GAIT, g=gref.G)
P_COMPASS = CompassParams(m=gref.M_LEG, m_h=gref.M_HIP, a=gref.A, b=gref.B,
                          gamma=gref.GAMMA_GAIT, g=gref.G)


def test_full_cycle_reduces_to_compass():
    """退化 rocker の全 stride 不動点・固有値が Phase 2 compass に一致。"""
    fp_c = find_limit_cycle(make_compass(P_COMPASS), np.array(gref.SECTION_GUESS))
    assert fp_c.converged
    fp_r = find_limit_cycle(make_rocker_compass(P_DEG), fp_c.y.copy())
    assert fp_r.converged
    assert np.allclose(fp_r.y, fp_c.y, atol=1e-5)
    mags_r = np.sort(np.abs(fp_r.eigenvalues))[::-1]
    mags_c = np.sort(np.abs(fp_c.eigenvalues))[::-1]
    assert np.allclose(mags_r, mags_c, atol=2e-3)
```

- [ ] **Step 2: 実行**

Run: `uv run pytest tests/test_rocker_compass_limit.py -v` → 1 PASSED
**FAIL 時**: Task 3 の力学/衝突一致テストに戻る（不動点ズレは個別写像のズレが原因）。
**勝手に許容値を緩めない**。

- [ ] **Step 3: Commit**

```bash
git add tests/test_rocker_compass_limit.py
git commit -m "test: rocker compass degenerates to verified compass (R->0 gate)"
```

---

### Task 6: リミットサイクル + 文献ゲート + 安定性

**Files:**
- Test: `tests/test_rocker_cycle.py`

- [ ] **Step 1: ゲートテストを書く**

```python
# tests/test_rocker_cycle.py
import numpy as np

from crane import references_mcgeer as ref
from crane.models.rocker_compass import RockerCompassParams, make_rocker_compass
from crane.search import find_limit_cycle
from crane.stride import stride

P = RockerCompassParams(m=ref.M_LEG, m_h=ref.M_HIP, c=ref.C_HIP_TO_COM,
                        rho=ref.RHO_GYR, L=ref.L_LEG, R=ref.R_FOOT,
                        gamma=ref.GAMMA_GAIT, g=ref.G)
MODEL = make_rocker_compass(P)


def test_rocker_limit_cycle_exists_at_published_config():
    fp = find_limit_cycle(MODEL, np.array(ref.SECTION_GUESS))
    assert fp.converged
    if ref.PUBLISHED_STABLE:
        assert np.max(np.abs(fp.eigenvalues)) < 1.0


def test_rocker_gait_matches_published_quantities():
    fp = find_limit_cycle(MODEL, np.array(ref.SECTION_GUESS))
    assert fp.converged
    result = stride(MODEL, MODEL.lift(fp.y))
    if ref.STEP_PERIOD is not None:
        assert np.isclose(result.t_step, ref.STEP_PERIOD, rtol=ref.GAIT_TOLERANCE)
    if ref.POINCARE_EIGENVALUES is not None:
        ours = np.sort(np.abs(fp.eigenvalues))
        theirs = np.sort(np.abs(np.array(ref.POINCARE_EIGENVALUES)))
        assert np.allclose(ours, theirs, rtol=ref.GAIT_TOLERANCE)
```

- [ ] **Step 2: 実行・探索**

収束しない場合のフォールバック（決定論的）:
1. R=1e-9（= Phase 2 不動点）から R を文献値まで少しずつ上げる continuation で初期推測を供給
2. SECTION_GUESS 近傍の粗グリッドで ‖S(y)−y‖ 最小点を seed に
3. 失敗モード（即時 strike / 転倒 / イベント未検出）を軌跡付きで報告

不動点・固有値・step period を必ず報告。SECTION_GUESS を補正したら references_mcgeer.py に
根拠コメント付きで更新。

- [ ] **Step 3: 小さな continuation テスト**

```python
def test_gait_family_continues_in_foot_radius():
    """R=0 (= Phase 2 不動点) から文献 R まで continuation で gait family を追える。"""
    from crane import references_goswami as gref
    y = np.array(gref.SECTION_GUESS)
    fp0 = None
    for frac in np.linspace(0.0, 1.0, 6):
        p = RockerCompassParams(m=ref.M_LEG, m_h=ref.M_HIP, c=ref.C_HIP_TO_COM,
                                rho=ref.RHO_GYR, L=ref.L_LEG,
                                R=max(ref.R_FOOT * frac, 1e-9),
                                gamma=ref.GAMMA_GAIT, g=ref.G)
        fp = find_limit_cycle(make_rocker_compass(p), y)
        if not fp.converged:
            break
        y, fp0 = fp.y, fp
    assert fp0 is not None and fp0.converged
```

（注: c, ρ, L が Phase 2 と異なる場合、R=0 出発点も Phase 2 と厳密一致しない。
その場合は McGeer パラメータでの粗グリッド seed に切替え、family の追跡範囲を報告）

- [ ] **Step 4: Commit**

```bash
git add tests/test_rocker_cycle.py src/crane/references_mcgeer.py
git commit -m "feat: rocker compass limit cycle gated on McGeer 1990a"
```

---

### Task 7: viz（円弧足）+ walk_rocker.py デモ

**Files:**
- Modify: `src/crane/viz.py`（`rocker_joints` + `animate_rocker` 追加）
- Create: `scripts/walk_rocker.py`
- Test: `tests/test_viz_rocker.py`

- [ ] **Step 1: テストを書く（関節・接触幾何）**

```python
# tests/test_viz_rocker.py
import numpy as np

from crane.viz import rocker_joints


def test_rocker_contact_at_ground():
    """stance 円弧足の接触点が地面 (y=0) にある。"""
    pts = rocker_joints(np.array([0.2, -0.2, 0, 0]), L=1.0, R=0.3, contact_x=0.0)
    # pts = (stance_contact, stance_C, hip, swing_C, swing_lowest)
    assert np.isclose(pts[0][1], 0.0, atol=1e-12)


def test_rocker_curvature_center_height_R():
    """stance 曲率中心 C は高さ R。"""
    pts = rocker_joints(np.array([0.2, -0.2, 0, 0]), L=1.0, R=0.3, contact_x=0.0)
    assert np.isclose(pts[1][1], 0.3, atol=1e-12)
```

- [ ] **Step 2: viz.py に実装**

```python
def rocker_joints(x: np.ndarray, L: float, R: float, contact_x: float):
    """rocker compass の関節座標（slope frame）。stance 接触点を (contact_x, 0) に。

    返り値: (stance_contact, stance_C, hip, swing_C, swing_lowest)
    """
    th_st, th_sw = x[0], x[1]

    def down(theta):
        return np.array([np.sin(theta), -np.cos(theta)])

    C_st = np.array([contact_x, R])
    hip = C_st - (L - R) * down(th_st)
    C_sw = hip + (L - R) * down(th_sw)
    swing_lowest = C_sw + np.array([0.0, -R])
    return np.array([contact_x, 0.0]), C_st, hip, C_sw, swing_lowest
```

`animate_rocker(strides, L, R, gamma, out, fps=30)`: animate_kneed と同じ構造。
stance 脚を hip→C_st→接触点（円弧足を弧で描画、`matplotlib.patches.Arc` か接触点弧の
polyline）、swing 脚を hip→C_sw→swing 足弧。stance 接触点アンカーは heel-strike 時の
swing 接触点 x（`rocker_joints(x_strike,...)` の swing 足接触 x）で前進。全体 −γ 回転。

- [ ] **Step 3: walk_rocker.py を書く**

walk_compass.py 骨格（argparse: --gamma-deg default=rad2deg(GAMMA_GAIT), --strides 30,
--perturb 0.005; converged ガード + 不安定警告 + meta.json に stable/R を記録）。
deviation = `np.linalg.norm(model.project(result.x_end) - fp.y)`。
fp は `find_limit_cycle(model, np.array(ref.SECTION_GUESS))`。

- [ ] **Step 4: 実行確認 + 全テスト + ruff + Commit**

Run: `uv run python scripts/walk_rocker.py` → mp4/png/meta 生成、deviation 減衰、
円弧足が転がる様子が見えること。

```bash
git add src/crane/viz.py tests/test_viz_rocker.py scripts/walk_rocker.py
git commit -m "feat: rocker compass walk demo with arc-foot animation"
```

---

### Task 8: ドキュメント更新 + 歩容判定ゲート

**Files:**
- Modify: `GOALS.md`, `README.md`

- [ ] **Step 1: GOALS.md に Phase 3.5 節を実績で記入**

Phase 1-3 と同じ形式: チェックリスト（実測数値入り: 不動点・固有値・step period・足半径 R）、
知見、今後の課題（kneed + rocker = 次候補）。最後に
`- [ ] **ぽんぽこ殿の歩容判定**: walk.mp4 が「円弧足で転がりながら歩く」ように見える` を未チェックで。

- [ ] **Step 2: README.md に Phase 3.5 結果を追記 + walk_rocker.py の使い方 + テスト数更新 + Commit**

```bash
git add GOALS.md README.md
git commit -m "docs: record phase 3.5 results and gait judgment gate"
```

- [ ] **Step 3: 歩容判定ゲート（人間ゲート — STOP）**

`data/runs/<ts>_rocker_*/walk.mp4` をぽんぽこ殿に提示。
**円弧足歩行の判定が出るまで kneed + rocker 検討に進まない。**

---

## Self-Review Notes

- 設計書（`docs/2026-06-16-rocker-foot-compass-design.md`）の5要件をカバー:
  スコープ＆ゲート(Task 1,6,8) / 検証アンカー McGeer(Task 1,6) / 物理モデル＝転がり剛体脚(Task 2,3) /
  コード構成(全 Task) / 三段防御(不変量 Task 2-4 → 退化ゲート Task 5 → 文献ゲート Task 6 → 歩容 Task 8)
- **最大リスク = 衝突 pivot の bookkeeping**（転がり frame）。対策: Task 3 で R→0 力学＆衝突一致を
  個別検証 → Task 5 で全 stride 退化ゲート。符号/pivot ミスはここで必ず捕捉される
- derive レイヤー（compass/kneed で検証済み）は**無改変**。2 質点表現で剛体慣性を再現し、
  point-mass の `angular_momentum`/`derive_qdd` をそのまま使う
- 型一貫性: `RockerCompassParams(m,m_h,c,rho,L,R,gamma,g)`、`_args` 順序、
  `dynamics/energy/kinetic_energy/heelstrike_map/make_rocker_compass` — Task 2-4 で統一
- 文献数値はプランに書かない。退化ゲートの照合先は Phase 2 実測 (0.27103, −1.09238, −0.37737) のみ
- McGeer の固有値が test machine（膝あり）由来で直脚モデルと不一致なら、Task 1 の判断で
  「存在＋安定性＋R→0 退化」を主ゲートにし固有値は参考比較へ縮退（GAIT_TOLERANCE/None で吸収）
```
